# Model Order Reduction Tool Requirements
# Python 3.9 compatible versions

# Core dependencies
numpy==1.24.4
pandas==2.0.3
matplotlib==3.7.2
streamlit==1.40.1

# Scientific computing
scipy==1.10.1
scikit-learn==1.3.0

# 3D visualization and VTK support
pyvista==0.43.0
vtk==9.2.6

# GUI support for PyVista interactive windows
PyQt5>=5.15.0; platform_system=="Windows"
PyQt5>=5.15.0; platform_system=="Darwin"

# OpenGL support (optional, for advanced 3D features)
PyOpenGL>=3.1.0
PyOpenGL-accelerate>=3.1.0

# 云环境支持 (Linux)
xvfbwrapper>=0.2.9; platform_system=="Linux"

# Image processing
pillow==9.5.0

# Deep learning (for neural network features)
torch>=2.0.0
torchvision>=0.15.0

# Model reduction library
ezyrb==1.3.0.post2404

# Other required dependencies
kiwisolver==1.4.4
joblib>=1.4.2
threadpoolctl>=3.5.0

# Standard library dependencies (usually included with Python)
# pathlib2 - 使用内置pathlib
# tempfile - 内置模块
# warnings - 内置模块
# os - 内置模块
# sys - 内置模块
# math - 内置模块
# time - 内置模块
# subprocess - 内置模块
# tracemalloc - 内置模块

# Windows specific (optional)
msvc-runtime>=14.0; platform_system=="Windows"
