# Model Order Reduction Tool Requirements
# Cloud deployment optimized

# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<3.0.0
matplotlib>=3.5.0,<4.0.0
streamlit>=1.28.0

# Scientific computing
scipy>=1.7.0,<2.0.0
scikit-learn>=1.0.0,<2.0.0

# 3D visualization (cloud-optimized)
pyvista>=0.38.0
vtk>=9.0.0

# GUI support for PyVista interactive windows (local only)
PyQt5>=5.15.0; platform_system=="Windows"
PyQt5>=5.15.0; platform_system=="Darwin"

# 云环境支持 (Linux)
xvfbwrapper>=0.2.9; platform_system=="Linux"

# Image processing
pillow>=8.0.0

# Deep learning (CPU-only for cloud)
torch>=1.12.0,<3.0.0
torchvision>=0.13.0,<1.0.0

# Model reduction library
ezyrb>=1.2.0

# Other required dependencies
joblib>=1.0.0
threadpoolctl>=3.0.0

# Standard library dependencies (usually included with Python)
# pathlib2 - 使用内置pathlib
# tempfile - 内置模块
# warnings - 内置模块
# os - 内置模块
# sys - 内置模块
# math - 内置模块
# time - 内置模块
# subprocess - 内置模块
# tracemalloc - 内置模块

# Windows specific (optional)
msvc-runtime>=14.0; platform_system=="Windows"
